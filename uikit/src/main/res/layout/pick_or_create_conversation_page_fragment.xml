<?xml version="1.0" encoding="utf-8"?>
<!--
    「选一个会话（转发、会议邀请）」页的内容布局。

    与 pick_or_create_conversation_activity.xml 逐行相同，只去掉了顶部那条 toolbar：
    标题栏由宿主提供 —— 手机端是 WfcBaseActivity，平板右栏是 PanePageFragment，
    页面自己不再包含它，于是同一份布局两端通用。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/containerFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="50dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="#ffffff"
        android:elevation="8dp"
        android:gravity="center"
        android:orientation="horizontal">

        <cn.wildfire.chat.kit.widget.MaxSizeRecyclerView
            android:id="@+id/selectedAvatarsRecyclerView"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:paddingLeft="16dp"
            app:maxWidth="300dp" />

        <EditText
            android:id="@+id/searchEditText"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@null"
            android:drawableLeft="@mipmap/ic_search1"
            android:drawablePadding="10dp"
            android:hint="@string/search"
            android:paddingLeft="10dp"
            android:textColorHint="@color/gray2"
            android:textSize="14sp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/multiSelectActionLayout"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_gravity="bottom"
        android:background="@color/white"
        android:elevation="8dp"
        android:orientation="horizontal"
        android:visibility="gone">

        <TextView
            android:id="@+id/selectedCountTextView"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:paddingLeft="16dp" />

        <Button
            android:id="@+id/sendButton"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginRight="8dp"
            android:enabled="false" />
    </LinearLayout>
</FrameLayout>
